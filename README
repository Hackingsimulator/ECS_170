Uploaded is the sources code for the model, userinterface(FrontEnd), and the dataset we used.
Please uplaod file to jupyter notebook and put dataset in same directory so it is readable. The code is pretty straightforward. 
